# Runtime
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
sqlalchemy>=2.0.36,<3.0
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0

# HTTP clients.
#   httpx  - used by the MCP server to call TMDB; also what the `mcp` package depends on.
#   httpx2 - Starlette >=1.3 wants this for TestClient. With only httpx installed,
#            every test run emits StarletteDeprecationWarning. Owner C cannot write
#            an API test without one of these.
httpx>=0.27,<1.0
httpx2>=2.9,<3.0

# Testing
pytest>=8.3,<9.0
